#!/usr/bin/env amm

import $ivy.`org.gnieh::diffson-circe:2.2.1`, gnieh.diffson.circe._

val variables = read! wd/"variables.json"
val variablesClm = read! wd/"variables-clm.json"
val patch = JsonDiff.diff(variables, variablesClm, false)

write(wd/"clm-patch.json", patch.toString)
